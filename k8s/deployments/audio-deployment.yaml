apiVersion: apps/v1
kind: Deployment
metadata:
  name: audio-service
  namespace: quran
  labels:
    app: audio-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audio-service
  template:
    metadata:
      labels:
        app: audio-service
    spec:
      containers:
      - name: audio-service
        image: quran/audio-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
          name: http
        envFrom:
        - configMapRef:
            name: quran-config
        - secretRef:
            name: quran-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: data
          mountPath: /data
        - name: audio
          mountPath: /audio
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: quran-data-pvc
      - name: audio
        persistentVolumeClaim:
          claimName: quran-audio-pvc

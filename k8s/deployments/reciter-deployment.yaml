apiVersion: apps/v1
kind: Deployment
metadata:
  name: reciter-service
  namespace: quran
  labels:
    app: reciter-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reciter-service
  template:
    metadata:
      labels:
        app: reciter-service
    spec:
      containers:
      - name: reciter-service
        image: quran/reciter-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3006
          name: http
        envFrom:
        - configMapRef:
            name: quran-config
        - secretRef:
            name: quran-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: quran-data-pvc

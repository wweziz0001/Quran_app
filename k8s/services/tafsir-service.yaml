apiVersion: v1
kind: Service
metadata:
  name: tafsir-service
  namespace: quran
  labels:
    app: tafsir-service
spec:
  type: ClusterIP
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    name: http
  selector:
    app: tafsir-service

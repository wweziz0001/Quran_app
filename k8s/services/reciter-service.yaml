apiVersion: v1
kind: Service
metadata:
  name: reciter-service
  namespace: quran
  labels:
    app: reciter-service
spec:
  type: ClusterIP
  ports:
  - port: 3006
    targetPort: 3006
    protocol: TCP
    name: http
  selector:
    app: reciter-service

apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: quran
  labels:
    app: search-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: search-service

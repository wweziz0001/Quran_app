apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: quran
  labels:
    app: ai-service
spec:
  type: ClusterIP
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
    name: http
  selector:
    app: ai-service

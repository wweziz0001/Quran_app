apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: quran
  labels:
    app: users-service
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: users-service

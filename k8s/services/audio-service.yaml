apiVersion: v1
kind: Service
metadata:
  name: audio-service
  namespace: quran
  labels:
    app: audio-service
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: audio-service

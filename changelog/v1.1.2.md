# الإصدار 1.1.2

**تاريخ الإصدار:** 2026-02-27  
**نوع الإصدار:** Patch (تحسينات تصميم)

---

## 🎨 تحسينات التصميم

### تحسين شكل أرقام الآيات في وضعية القائمة

**الوصف:**  
تم تحديث تصميم أرقام الآيات في مستعرض القرآن الكريم (وضعية القائمة) لتكون متناسقة مع تصميم أرقام السور في الفهرس.

**المشكلة السابقة:**
- كانت أرقام الآيات دائرية الشكل
- لم تكن متناسقة مع تصميم الفهرس

**الحل:**
- تغيير الشكل من دائري إلى مربع مع حواف مستديرة (rounded-xl)
- إضافة خلفية متدرجة (gradient) مشابهة لخلفية أرقام السور
- إضافة زاوية مزخرفة صغيرة في الأعلى (decorative corner)
- تحسين الظلال والتوهج لتتناسب مع الوضع الداكن والفاتح

**الملف:** `src/components/quran/islamic-ornaments.tsx`

---

## التغييرات التفصيلية

### قبل التحديث (دائري):
```tsx
// Luxurious Verse Number
<span className="inline-flex items-center justify-center mx-3 relative align-middle">
  {/* Outer glow */}
  <span className="absolute w-12 h-12 rounded-full blur-sm ..." />
  
  {/* Decorative ring */}
  <span className="absolute w-11 h-11 rounded-full ..." />
  
  {/* Inner circle with number */}
  <span className="relative w-10 h-10 rounded-full flex items-center justify-center ...">
    {toArabicNumber(number)}
  </span>
</span>
```

### بعد التحديث (مربع مثل الفهرس):
```tsx
// Luxurious Verse Number - Matching Surah Index Style
<span className="inline-flex items-center justify-center mx-2 relative align-middle">
  {/* Main container - Square like Surah Index */}
  <span className="relative w-11 h-11 rounded-xl flex items-center justify-center shrink-0 ...">
    <span className="text-sm font-bold" style={{ fontFamily: "'Scheherazade New', 'Amiri', serif" }}>
      {toArabicNumber(number)}
    </span>
    {/* Decorative corner */}
    <span className="absolute -top-1 -right-1 w-3 h-3 rotate-45 ..." />
    {/* Inner border glow */}
    <span className="absolute inset-0 rounded-xl ..." />
  </span>
</span>
```

---

## 📊 إحصائيات الإصدار

| المقياس | القيمة |
|---------|--------|
| ملفات مصححة | 1 |
| سطور مضافة | ~15 |
| سطور محذوفة | ~20 |
| تحسينات تصميم | 1 |

---

## 🔗 مقارنة مع أرقام السور

### أرقام السور (الفهرس):
```tsx
<div className="relative w-12 h-12 rounded-xl flex items-center justify-center shrink-0
  bg-gradient-to-br from-emerald-400 to-teal-600 text-white
  shadow-lg shadow-emerald-500/30">
  <span className="text-sm font-bold">{toArabicNumber(surah.number)}</span>
  <div className="absolute -top-1 -right-1 w-3 h-3 rotate-45 bg-white/30" />
</div>
```

### أرقام الآيات (بعد التحديث):
```tsx
<span className="relative w-11 h-11 rounded-xl flex items-center justify-center shrink-0
  bg-gradient-to-br from-emerald-400 via-emerald-500 to-teal-600 text-white
  shadow-lg shadow-emerald-500/30">
  <span className="text-sm font-bold">{toArabicNumber(number)}</span>
  <span className="absolute -top-1 -right-1 w-3 h-3 rotate-45 bg-white/30" />
</span>
```

**النتيجة:** تصميم موحد ومتناسق بين الفهرس والمستعرض.
